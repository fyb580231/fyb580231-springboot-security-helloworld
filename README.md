# SpringBoot 3.2.0 + Security JWT HelloWorld

## 功能
1. 登录接口：`POST /api/auth/login`（用户名 test，密码 123456）
2. 受保护接口：`GET /api/hello`（需携带 JWT 令牌）

## 运行方式
1. 克隆仓库：
   ```bash
   git clone https://github.com/your-username/springboot-security-helloworld.git
